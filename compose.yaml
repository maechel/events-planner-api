services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=event_planner'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
